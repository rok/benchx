"""benchx prototype: the slice of docs/design/prototype-design.md.

One module per component behind the `bx` command line: core (documents),
snapshot (what is recorded), adapters.gbench, runner, identity, parquet,
store, compare.
"""

__version__ = "0.1.0"
