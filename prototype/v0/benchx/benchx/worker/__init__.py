"""The worker: executes one work order and writes measurement results.

See docs/design/worker-prototype.md. The pipeline lives in `run`, harness
adapters in `benchx.adapters`.
"""

__version__ = "0.1.0"
